```angular2html
ChatLayout (viewport - header height)
├── Sidebar
│   ├── New Chat Button (fixed height)
│   └── Chat List (scrollable)
└── Main Content
    ├── Message Area (scrollable, flex-1)
    └── Input Area (fixed height)
```